apiVersion: v1
kind: Service
metadata:
  labels:
    app: spoon-backend
  name: spoon-backend
  namespace: spoon
spec:
  type: LoadBalancer
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: spoon-backend